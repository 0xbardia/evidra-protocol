import { pageMetadata } from '../../../lib/seo';

export const metadata = pageMetadata('Developer reference', 'Use the frozen Studio Dev deployment, normalized API, shared protocol client, and user-approved wallet writes.', '/app/developers');

export default function DevelopersLayout({ children }: Readonly<{ children: React.ReactNode }>) { return children; }
